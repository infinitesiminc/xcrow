import WorkspaceSettings from "./WorkspaceSettings";

// InvitePage is an alias — the join code management lives in WorkspaceSettings
export default function InvitePage() {
  return <WorkspaceSettings />;
}
